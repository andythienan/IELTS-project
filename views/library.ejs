<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library - Learn English</title>
    <link rel="stylesheet" href="/styles/library.css">
    <link rel="stylesheet" href="/styles/typography.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body class="page-library">
    <header class="header">
        <%- include('partials/header', { user: name }) %>
    </header>

    <main>
        <aside class="sidebar">
            <h1>
                <% if (type === 'exam') { %>
                Categories
                <% } else { %>
                Lesson topics
                <% } %>
            </h1>
            <ul>
                <% if (type === 'exam') { %>
                <li><a href="#listening-section">Listening</a></li>
                <li><a href="#reading-section">Reading</a></li>
                <li><a href="#writing-section">Writing</a></li>
                <li><a href="#speaking-section">Speaking</a></li>
                <% } else { %>
                <li><a href="#grammar-section">Grammar</a></li>
                <li><a href="#vocabulary-section">Vocabulary</a></li>
                <li><a href="#phrasal-verbs-section">Phrasal verbs</a></li>
                <li><a href="#idioms-section">Idioms</a></li>
                <li><a href="#expressions-section">Expressions</a></li>
                <% } %>
            </ul>
        </aside>

        <div class="divider"></div>

        <section class="content">
            <!-- Announcement Section -->
            <section class="announcement">
                <h2>
                    <% if (type === 'exam') { %>
                    New exams added!
                    <% } else { %>
                    New lessons added!
                    <% } %>
                </h2>
                <p>
                    <% if (type === 'exam') { %>
                    Get ready to elevate your skills! 🚀 Our brand-new interactive tests for <b>Reading</b> and
                    <b>Writing</b> are here to challenge and inspire you! 🎉 Test your knowledge today and stay
                    tuned—more exciting tests are just around the corner! 🔥
                    <% } else { %>
                    Dive into our newest lesson library updates! 🌟 From mastering grammar to unlocking idiomatic expressions, we’ve got all the tools you need to become fluent in English. Start your journey today! 📚✨
                    <% } %>
                </p>
            </section>

            <!-- Introduction Section -->
            <section class="category">
                <h1>
                    <% if (type === 'exam') { %>
                    Exam library
                    <% } else { %>
                    Lesson library
                    <% } %>
                </h1>
                <p>
                    <% if (type === 'exam') { %>
                    Simple to use, simple to adopt. Our exam library is designed to meet the standard scope and sequence
                    requirements of their respective courses – and are 100% free online. Complete with free resources
                    for educators (like LMS integration, lecture slides, instructor answer keys, and more), all tests
                    are fully customizable. Explore our library below and add any and all parts useful to your course.
                    <% } else { %>
                    Find carefully curated lessons tailored to help you improve your English skills. Browse through a variety of topics and learn at your own pace. Our resources are 100% free and accessible anytime.
                    <% } %>
                </p>
            </section>

            <% if (type === 'exam') { %>
                <!-- Listening Section -->
                <section class="category" id="listening-section">
                    <h2>Listening</h2>
                    <p>Our listening tests are 100% free, customizable, and can be accessed anytime, anywhere. Did we also
                        mention that they come with free resources for educators and students? Check them out below!</p>
                    <div class="grid">
                        <% data.listening.forEach(exam => { %>
                            <div class="card exam <%= exam.available ? 'available' : 'unavailable' %>">
                                <h3><%= exam.title %> <span class="tag"><%= exam.available ? 'Available' : 'Coming soon' %></span></h3>
                                <% if (exam.available) { %>
                                    <div class="dropdown exam-dropdown">
                                        <button class="btn btn-primary">Take a look <span
                                                class="material-icons">keyboard_arrow_down</span></button>
                                        <div class="dropdown-content exam-dropdown-content">
                                            <a href="<%= exam.url %>"><span class="material-icons">visibility</span> Take
                                                online</a>
                                            <a href="<%= exam.pdfUrl %>"><span class="material-icons">picture_as_pdf</span> View
                                                in PDF</a>
                                        </div>
                                    </div>
                                <% } else { %>
                                    <button class="btn btn-secondary" disabled>Coming soon</button>
                                <% } %>
                            </div>
                        <% }); %>
                    </div>
                </section>

                <!-- Reading Section -->
                <section class="category" id="reading-section">
                    <h2>Reading</h2>
                    <p>Our free reading tests are free, flexible, and can be viewed online, downloaded as a PDF, and come
                        with additional free educational resources. Check them out below!</p>
                    <div class="grid">
                        <% data.reading.forEach(exam => { %>
                            <div class="card exam <%= exam.available ? 'available' : 'unavailable' %>">
                                <h3><%= exam.title %> <span class="tag"><%= exam.available ? 'Available' : 'Coming soon' %></span></h3>
                                <% if (exam.available) { %>
                                    <div class="dropdown exam-dropdown">
                                        <button class="btn btn-primary">Take a look <span
                                                class="material-icons">keyboard_arrow_down</span></button>
                                        <div class="dropdown-content exam-dropdown-content">
                                            <a href="<%= exam.url %>"><span class="material-icons">visibility</span> Take
                                                online</a>
                                            <a href="<%= exam.pdfUrl %>"><span class="material-icons">picture_as_pdf</span> View
                                                in PDF</a>
                                        </div>
                                    </div>
                                <% } else { %>
                                    <button class="btn btn-secondary" disabled>Coming soon</button>
                                <% } %>
                            </div>
                        <% }); %>
                    </div>
                </section>

                 <!-- Writing Section -->
                <section class="category" id="writing-section">
                    <h2>Writing</h2>
                    <p>Our free writing tests are free, flexible, and can be viewed online, downloaded as a PDF, and come
                        with additional free educational resources. Check them out below!</p>
                    <div class="grid">
                        <% data.writing.forEach(exam => { %>
                            <div class="card exam <%= exam.available ? 'available' : 'unavailable' %>">
                                <h3><%= exam.title %> <span class="tag"><%= exam.available ? 'Available' : 'Coming soon' %></span></h3>
                                <% if (exam.available) { %>
                                    <div class="dropdown exam-dropdown">
                                        <button class="btn btn-primary">Take a look <span
                                                class="material-icons">keyboard_arrow_down</span></button>
                                        <div class="dropdown-content exam-dropdown-content">
                                            <a href="<%= exam.url %>"><span class="material-icons">visibility</span> Take
                                                online</a>
                                            <a href="<%= exam.pdfUrl %>"><span class="material-icons">picture_as_pdf</span> View
                                                in PDF</a>
                                        </div>
                                    </div>
                                <% } else { %>
                                    <button class="btn btn-secondary" disabled>Coming Soon</button>
                                <% } %>
                            </div>
                        <% }); %>
                    </div>
                </section>


               <!-- Speaking Section -->
                <section class="category" id="speaking-section">
                    <h2>Speaking</h2>
                    <p>Our free speaking tests are free, flexible, and can be viewed online, downloaded as a PDF, and come
                        with additional free educational resources. Check them out below!</p>
                    <div class="grid">
                      <% data.speaking.forEach(exam => { %>
                            <div class="card exam <%= exam.available ? 'available' : 'unavailable' %>">
                                <h3><%= exam.title %> <span class="tag"><%= exam.available ? 'Available' : 'Coming soon' %></span></h3>
                                <% if (!exam.available) { %>
                                    <button class="btn btn-secondary" disabled>Coming soon</button>
                                <% } %>
                            </div>
                        <% }); %>
                    </div>
                </section>

                <% } else { %>
                   <!-- Grammar Lessons -->
                    <section class="category" id="grammar-section">
                        <h2>Grammar</h2>
                        <p>Master the building blocks of English with our comprehensive grammar lessons.</p>
                        <div class="grid">
                            <% data.grammar.forEach(lesson => { %>
                                <div class="card lesson <%= lesson.available ? 'available' : 'unavailable' %>">
                                    <h3><%= lesson.title %></h3>
                                    <% if (lesson.available) { %>
                                        <a href="<%= lesson.url %>" class="btn btn-primary">Start lesson</a>
                                    <% } else { %>
                                        <button class="btn btn-secondary" disabled>Coming Soon</button>
                                    <% } %>
                                </div>
                            <% }); %>
                        </div>
                    </section>

                      <!-- Vocabulary Lessons -->
                    <section class="category" id="vocabulary-section">
                        <h2>Vocabulary</h2>
                        <p>Expand your word bank with lessons focused on commonly used English words and phrases.</p>
                        <div class="grid">
                            <% data.vocabulary.forEach(lesson => { %>
                                <div class="card lesson <%= lesson.available ? 'available' : 'unavailable' %>">
                                    <h3><%= lesson.title %></h3>
                                     <% if (lesson.available) { %>
                                        <a href="<%= lesson.url %>" class="btn btn-primary">Start lesson</a>
                                    <% } else { %>
                                        <button class="btn btn-secondary" disabled>Coming Soon</button>
                                    <% } %>
                                </div>
                            <% }); %>
                        </div>
                    </section>
            <% } %>

        </section>
    </main>

    <footer class="footer">
        <%- include('partials/footer'); -%>
    </footer>

</body>

</html>